import * as React from 'react';

export default function OrganizationHomePage() {
  // This default page is needed for the layout's children slot,
  // even though the main content comes from parallel routes.
  // It doesn't need to render anything itself.
  return null;
} 